# deblasis/homebrew-tap

```sh
brew install deblasis/tap/zioscp
```

Homebrew tap for [zioscp](https://github.com/deblasis/zioscp) — a drop-in `scp`
replacement in Zig (resumable, parallel, pipelined SFTP). The formula installs
the prebuilt macOS binary.
